using System;

namespace Arrays_ {
    class Arrays {
        static void Main(string[] args) {
            var r = new Random();
            //int length = 6;
            //int[] arr = new int[length];


            //for (int i = 0; i < arr.Length; i++) {
            //    arr[i] = r.Next(10, 99);
            //}

            //foreach (var item in arr) {
            //    Console.Write(item + " ");
            //}
            //Console.WriteLine();

            //Console.WriteLine(arr.Rank);

            //Console.WriteLine(arr[^2]);

            //int[] arr2 = arr[2..4];
            //foreach (var item in arr2) {
            //    Console.Write(item + " ");
            //}

            // multidimension arrays

            //int[,] arr2D = new int[5, 7];
            ////Console.WriteLine(arr2D.Rank);
            ////Console.WriteLine(arr2D.Length);
            //int rows = arr2D.GetUpperBound(0) + 1;
            //int cols = arr2D.GetUpperBound(1) + 1;
            //for (int i = 0; i < rows; i++) {
            //    for (int j = 0; j < cols; j++) {
            //        arr2D[i, j] = r.Next(10, 100);
            //    }
            //}

            //for (int i = 0; i < rows; i++) {
            //    for (int j = 0; j < cols; j++) {
            //        Console.Write(arr2D[i, j] + " ");
            //    }
            //    Console.WriteLine();
            //}

            //Console.WriteLine(arr2D[0, 2]);
            //Console.WriteLine(cols);

            //double[][] jaggedArr = new double[5][];
            //jaggedArr[0] = new double[1];
            //jaggedArr[1] = new double[2];
            //jaggedArr[2] = new double[3];
            //jaggedArr[3] = new double[4];
            //jaggedArr[4] = new double[5];

            //for (int i = 0; i < 5; i++) {
            //    for (int j = 0; j < i + 1; j++) {
            //        jaggedArr[i][j] = Math.Round(r.NextDouble() * 100, 2);
            //    }
            //}

            //for (int i = 0; i < 5; i++) {
            //    for (int j = 0; j < i + 1; j++) {
            //        Console.Write(jaggedArr[i][j] + " ");
            //    }
            //    Console.WriteLine();
            //}          
        }
    }
}
